<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Register</title>
    <!--Import google icon font-->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="css/materialize.min.css"  media="screen,projection"/>
    <link rel="stylesheet" href="/css/style.css">
    <!--Let browser know website is optimized for mobile-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  </head>
  <body>
    <div class="container">
 <!--     <nav class="mynavbar">
        <div class="nav-wrapper">
          <a href="#" class="brand-logo left">Dawdle</a>
          <ul id="nav-mobile" class="right hide-on-med-and-down">
            <li><a href="/">Home page</a></li>
          </ul>
        </div>
      </nav>
 -->     <h3>
        Sign Up :
      </h3>
      <%if (errors) { %>
        <ul class="errors">
          <%for (var err in errors){%>
            <li class="alert alert-danger"> <%=errors[err]%></li><br>
          <%}%>
        </ul>
      <%}%>
      <% if ( messages ) { %>
        <%for (var msg in messages){%>
          <div class="flash"><%= messages[msg] %></div>
        <%}%>
      <% } %>

      <div class="row">
        <form class="col s12" action="/wregister" method="POST" enctype="url/encoded">
          <div class="row">
            <div class="input-field col s12">
              <i class="material-icons prefix">email</i>
              <input id="icon_prefix1" type="email" class="validate" name="email">
              <label for="icon_prefix1">Email</label>
            </div>
            <div class="input-field col s12">
              <i class="material-icons prefix">create</i>
              <input id="icon_prefix2" type="password" class="validate" name="password">
              <label for="icon_prefix2">Password</label>
            </div>
            <div class="input-field col s12">
              <i class="material-icons prefix">create</i>
              <input id="icon_prefix3" type="password" class="validate" name="password2">
              <label for="icon_prefix3">Confirm password</label>
            </div>
          </div>
          <button class="btn waves-effect waves-light" type="submit" name="action">Register
          <i class="material-icons right">navigate_next</i>
          </button>
        </form>
      </div>
      <br>


      <a href="/wlogin">Already have an account?</a>
    </div>
    <script src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
    <script type="text/javascript" src="/js/materialize.min.js"></script>
  </body>
</html>
